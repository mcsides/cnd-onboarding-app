import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const LandingPageFrame = styled(Box)(({ theme }) => ({
      backgroundColor: theme.palette.background.default,

}))

export default LandingPageFrame;